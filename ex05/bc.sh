#!/bin/bash

case "$1" in
  -s)
    #faire la somme de tous les nombres
;;
esac

case "$1" in
  -e)
    #faire la somme des nombres pairs
;;
esac

case "$1" in
  -o)
    # faire la somme des nombres impairs
;;
esac

case "$1" in
  -m)
    #faire la moyenne de tous les nombres
esac
